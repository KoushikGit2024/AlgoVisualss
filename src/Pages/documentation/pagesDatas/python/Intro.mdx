import CodeWindow from "../../components/parsers/CodeWindow";

# Hello from MDX!

This is a standard paragraph. It will automatically get the Tailwind styles we defined in the `MdxLayout` component. Here is a [cool link](https://google.com).

## Why is this awesome?
* You don't have to write JSON arrays anymore.
* You can write standard markdown.
* **You can inject React components anywhere!**

Look at this custom component below:

<CodeWindow 
  editorLanguage="python" 
  code={`print("MDX is finally working!")`} 
/>